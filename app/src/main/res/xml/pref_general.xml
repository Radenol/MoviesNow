<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="@string/sort_popular"
        android:entries="@array/pref_movie_sort_entries"
        android:entryValues="@array/pref_movie_sort_values"
        android:key="@string/pref_movie_sort_key"
        android:title="@string/pref_movie_sort_label" />

</PreferenceScreen>
